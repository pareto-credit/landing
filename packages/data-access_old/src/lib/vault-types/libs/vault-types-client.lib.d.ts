import { Axios } from 'axios';
import { VaultTypesClient } from '../vault-type.model';
/**
 * Create vault types client
 * @param axios - The Axios instance
 * @returns the vault type GOT client
 */
export declare function createVaultTypesClient(axios: Axios): VaultTypesClient;
